.DS_Store
*.swp
*.swo
.idea/
.vscode/
node_modules/

# Локальный бэкап старого лендинга FT — не публиковать (старые версии есть в истории git)
foundertrajectory/_archive-2026-06-11/

# Снимки портала /os/ — приватные данные, репозиторий публичный.
# Публикуются в хранилище через /os/api/data, в git не попадают.
os/data/

# Локальное состояние wrangler (кеш и эмулятор KV), в репозитории не нужно.
.wrangler/
